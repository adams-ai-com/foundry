DATABASE_URL=postgresql://foundry:password@localhost/foundry_workspace
# Optional SMTP for magic link emails — if unset, links are logged to stdout
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@foundry.local
APP_URL=http://localhost:3000
